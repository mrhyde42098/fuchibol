# Vacío en dev = proxy Vite a localhost:4000
# Servidor local (SERVE_FRONTEND): dejar vacío o usar /api
# Vercel/Netlify: URL completa del backend CON /api al final
# VITE_API_URL=https://fuchibol-api.onrender.com/api

VITE_API_URL=
